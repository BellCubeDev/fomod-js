# `src/tests`: End-To-End
The `src/tests` folder in this project contains almost exclusively E2E tests; testing the library from start to finish. Unit tests can be found next to the files they are testing.
